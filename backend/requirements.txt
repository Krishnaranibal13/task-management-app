# Backend runtime dependencies (Task Management MVP)
# Pinned for reproducible local development and CI.

fastapi==0.115.12
uvicorn[standard]==0.34.0

SQLAlchemy==2.0.40
PyMySQL==1.1.1
cryptography==44.0.2        # required by PyMySQL for MySQL 8 caching_sha2_password auth
alembic==1.15.2

pydantic==2.11.3
pydantic-settings==2.8.1

argon2-cffi==23.1.0         # Argon2id password hashing (approved algorithm)

# Reserved for later phases:
# python-multipart     # form parsing if needed
