# Base URL of the FastAPI backend.
# Local development only. In production prefer the same-origin topology
# (frontend and API served from the same origin behind one reverse proxy),
# so this can stay empty / relative.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
